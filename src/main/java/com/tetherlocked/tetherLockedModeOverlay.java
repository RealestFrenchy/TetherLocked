package com.tetherlocked;
import net.runelite.api.Client;
import net.runelite.api.Perspective;
import net.runelite.api.coords.LocalPoint;
import net.runelite.api.coords.WorldPoint;
import net.runelite.client.ui.overlay.*;
import net.runelite.client.ui.overlay.OverlayPanel;
import net.runelite.client.ui.overlay.OverlayPosition;
import net.runelite.client.ui.overlay.OverlayPriority;
/*
import javax.inject.Inject;
import java.awt.*;
import java.util.Collection;
public class tetherLockedModeOverlay extends Overlay {
    private static final int MAX_DRAW_DISTANCE = 32;

    private Client client;
    private TetherLockedPlugin plugin;

    @Inject
    private TetherLockedConfig config;

    @Inject
    private tetherLockedModeOverlay(Client client, TetherLockedConfig config, TetherLockedPlugin plugin)
    {
        this.client = client;
        this.plugin = plugin;
        this.config = config;
        setPosition(OverlayPosition.DYNAMIC);
        setPriority(OverlayPriority.LOW);
        setLayer(OverlayLayer.ABOVE_SCENE);
    }


}
*/